# Copy to `.env` for local development. Expo loads `EXPO_PUBLIC_*` at build time.
#
# Clerk (https://dashboard.clerk.com/~/api-keys). Enable Native API for this app in the dashboard.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=""

# Google sign-in uses Clerk OAuth (`useSSO`). Enable Google in the Clerk dashboard and add your
# mobile redirect URL (scheme + `/sso-callback`, e.g. ivymobileapp://sso-callback) under Allowed redirect URLs.

# Example: a public YouTube video or past live VOD used as the Watch tab placeholder.
EXPO_PUBLIC_YOUTUBE_PLACEHOLDER_VIDEO_ID=dQw4w9WgXcQ

# Base URL of the Next.js app (tRPC). Use your machine LAN IP when testing on a physical device.
# Public URL of the web app. Used to open the billing/pricing page in the in-app browser.
# In development, use your machine LAN IP (e.g. http://192.168.1.x:3000).
# In production, set to the deployed web app domain (e.g. https://ivy.app).
EXPO_PUBLIC_API_URL=http://localhost:3000