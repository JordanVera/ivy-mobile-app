# Optional: used by root `postinstall` / Prisma generate if you run tooling from the repo root.
# Prefer `apps/web/.env` for local Next.js + Prisma development.
DATABASE_URL="mysql://USER:PASSWORD@127.0.0.1:3306/ivy"
